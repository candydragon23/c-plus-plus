#include <iostream>
#include <math.h> // Подключение математических функций
#define _USE_MATH_DEFINES // Использование математических констант
using namespace std;
int main()
{
    abs(x) // Модуль
    sqrt(x) // Корень
    exp(x) // Экспонента в степени
    log(x), log10(x) // Логарифмы
    pow(x, y) // Степень
    sin(x), cos(x), tan(x), // Тригонометрия (x в радианах)
    asin(x), acos(x), atan(x) // Тригонометрия (результат в радианах)
    M_PI = 3.14...
    M_E = 2.718....
}
